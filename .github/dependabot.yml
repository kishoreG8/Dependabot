version: 2
updates:
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "daily"
    ignore:
      - dependency-name: "com.trimble:*"  # Ignore all Trimble-specific dependencies
    groups:
      gradle-dependencies:
        patterns:
          - "*"
