version: 2
updates:
  - package-ecosystem: "gradle"
    directory: "/"
    schedule:
      interval: "daily"
    ignore:
      - dependency-name: "com.trimble:*"  # Ignore all private dependencies
    groups:
      gradle-dependencies:
        patterns:
          - "*"
